# My Bills 20.0.133

This build locks the entire app to one theme system so older light-mode rules cannot override individual sections. Headers, dashboard cards, quick actions, budget/settings tiles, and bottom navigation use navy/cyan/white. Calendar cells, bills, forms, and spending ledger remain light for readability. Appearance now controls the final CSS variables used by the whole app.
